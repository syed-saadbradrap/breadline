Product and category photos live in `products/`.

Filenames are referenced from `src/data/products.ts` and `src/data/categories.ts`.
Replace any file with your own restaurant photo using the same name.
